// import { create } from "zustand";
// // import { createUserSlice, UserState } from './userSlice';
// // import { createSettingsSlice, SettingsState } from './settingsSlice';
// import { createUserSlice, UserState } from "./slices/useSlice";
// import { useAuthStore, AuthState } from "./slices/authSlice";

// type AppState = UserState;

// export const useAppStore = create<AppState>()((...a) => ({
//   ...createUserSlice(...a),
//   ...useAuthStore(),
// }));
